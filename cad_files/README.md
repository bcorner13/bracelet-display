# CAD Files

This directory contains the FreeCAD documents for the bracelet display project:

- `Params.FCStd` - Parameter document containing all VarSet variables
- `BraceletDisplay.FCStd` - Main model document with geometry
- `*.py` - Python scripts for parametric setup and automation

All .FCStd files should be parametrically linked to the Params document.