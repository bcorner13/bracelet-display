# Models

Versioned FreeCAD models per PROJECT_BOOTSTRAP.md standard:

- `BraceletDisplay_v1.0.0.FCStd` - Initial parametric model
- `BraceletDisplay_v1.1.0.FCStd` - Minor parameter adjustments
- `BraceletDisplay_v2.0.0.FCStd` - Major design changes

**Naming Convention:** `<ProjectName>_vX.Y.Z.FCStd`

**Save Policy:** Only save after human approval (never automatically)